
<div class="comment">
  <% if(locals.user) { %>
    <div class="addNewComment">
      <form action="/comment/create" method="post">
        <input type="text" name="content" id="" />
        <input type="hidden" name="post" value="<%= post._id %>" />
        <button type="submit" id="addNewComment">Add Comment</button>
      </form>
    </div>
  <% } %>
  <% for(comment of post.comments) {%>
    <div class="comment-header">
    <div class="comment-name">
      <img src="<%= comment.user.avatar %>" alt="Profile Picture" / id = "dp">
      <h4><%= comment.user.name %></h4>
    </div>
    <p>
      <%= comment.content %>
    </p>
  </div>
  <div class="comment-footer">
    <div class="left-section">
      <span>0 Likes</span>
      <a href="#"><img src="./images/like.png" alt="" id="like" /></a>
    </div>
    <p class="comment-details"><%= comment.createAt%></p>
  </div>
  <% } %>

  
  
  
  
</div>
